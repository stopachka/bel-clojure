sexp = string | list | quote | backquote | comma | splice | char | symbol | dot
list = lparen sexp? (<whitespace> sexp)* rparen
<lparen> = <'('>
<rparen> = <')'>
string = <'"'> any_char* <'"'>
char = <'\\'> name
symbol = name
dot = #'\.'
quote = <'\''> sexp
backquote = <'`'> sexp
comma = <','> sexp
splice = <',@'> sexp
name = #'[a-zA-Z!\-*+_]+'
any_char = #'.'
whitespace = #'\s+'
