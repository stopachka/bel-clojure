sexp = string | list | char | symbol | dot
list = lparen sexp (<whitespace> sexp)* rparen
<lparen> = <'('>
<rparen> = <')'>
string = <'"'> any_char* <'"'>
char = <'\\'> name
symbol = name
name = #'[a-zA-Z!\-*+_]+'
any_char = #'.'
whitespace = #'\s+'
dot = #'\.'
